const express = require("express")
const router = express.Router()
// const middleware = require("../config/middleware/index")
// const listRoutes = require("./routes")

// listRoutes["user"].map(x => router[x.method](x.route, [middleware.auth.checkToken, middleware.user.validate(x.func)], x.controllerModel))

module.exports = router
